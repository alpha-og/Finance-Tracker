import { TransactionsList } from "@/components/components";

const Dashboard = () => {
  return (
    <div className="w-full h-full flex justify-evenly">
      <h1>Dashboard</h1>
    </div>
  );
};

export default Dashboard;
